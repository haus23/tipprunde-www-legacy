// Global styles
import './styles/index.css';

const appContainer = document.getElementById('app') as HTMLElement;

const app = document.createElement('h1');
app.textContent = 'runde.tips';

appContainer.appendChild(app);
